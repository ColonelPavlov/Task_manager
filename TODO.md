# Чек-лист по ТЗ проекта Task Manager

## 🟨 1. Система регистрации пользователей, входа и JWT
> **Описание из ТЗ:** Реализовать систему регистрации и входа пользователей + JWT.
* [x] **[Backend(Я)]** Написан роут `GET /login/{username}` для проверки входа. - **ЗАГЛУШКА** *(работает на статических условиях без БД)*
* [x] **[Backend(Я)]** Написан роут `POST /register` для приёма данных новых пользователей. - **ЗАГЛУШКА** *(принимает данные через Pydantic, временно возвращает пароль в debug_info)*
* [ ] **[Database Engineer(Егор)]** Подключить базу данных для проверки существования пользователей и записи новых строк. - **ОТЛОЖЕНО** *(ожидаем развёртывания БД)*
* [ ] **[Backend(Я)]** Написать логику генерации и валидации JWT-токенов после проверки паролей. - **ОТЛОЖЕНО** *(пишется после интеграции БД)*

---

## 🟥 2. Структура и нормализация Базы Данных
> **Описание из ТЗ:** В базе должно быть не менее трех связанных между собой сущностей, база данных должна быть нормализована, как минимум по трем формам.
* [ ] **[Database Engineer(Егор)]** Развернуть СУБД. - **ОТЛОЖЕНО** *(работа инженера БД)*
* [ ] **[Database Engineer(Егор)]** Спроектировать 3+ связанные таблицы (например: `users`, `tasks`, `categories`) строго по правилам 3-й нормальной формы (3НФ). - **ОТЛОЖЕНО** *(работа инженера БД)*
* [ ] **[Database Engineer(Егор)]** Подготовить чистые SQL-скрипты `CREATE TABLE` без использования ORM. - **ОТЛОЖЕНО** *(работа инженера БД)*
* [ ] **[Backend(Я)]** Написать модуль `database.py` или типо того, для подключения к готовой базе данных и выполнения сырых SQL-запросов. - **ОТЛОЖЕНО** *(моя задача после готовности БД)*

---

## 🟥 3. Резервные ответы (Fallback)
> **Описание из ТЗ:** Иметь резервные ответы, в случае если сервисы, которые вы используете, не доступны.
* [ ] **[Backend(Я)]** Написать блоки `try/except` на случай, если Core не сможет достучаться до Supporting (или наоборот), чтобы приложение выдавало резервный JSON-ответ вместо падения с ошибкой 500. - **ОТЛОЖЕНО** *(реализуется в самом конце работы при связывании сервисов)*

---

## 🟩 4. Реализовать два сервиса различными фреймворками
> **Описание из ТЗ:** Сервис Core/Business (Основная бизнес-логика и работа с главными сущностями) + Сервис Supporting (Вспомогательный сервис для API Gateway, отчетность / аналитика / авторизации / уведомлений в json формате).
* [x] **[Backend(Я)]** Создан и запущен сервис **Core** на FastAPI (Локальный порт `8000`). - **ВЫПОЛНЕНО**
* [x] **[Backend(Я)]** Создан и запущен сервис **Supporting** на Flask (Локальный порт `5001`). - **ВЫПОЛНЕНО**
* [x] **[Backend(Я)]** Оба сервиса настроены на отдачу ответов строго в JSON формате. - **ВЫПОЛНЕНО**
* [ ] **[DevSecOps / DB Engineer(Егор)]** Развернуть и настроить **Nginx** в роли **API Gateway**. Он должен слушать внешние порты (80/443) и перенаправлять трафик на внутренние порты FastAPI (`8000`) и Flask (`5001`), скрывая их от прямого доступа из интернета. - **ОТЛОЖЕНО** *(задача для инфраструктуры)*

---

## 🟥 5. Поиск, фильтрация, сортировка и пагинация
> **Описание из ТЗ:** Реализовать поиск с фильтрацией, сортировкой и пагинацией, на уровне запросов в БД.
* [ ] **[Database Engineer(Егор)]** Написать SQL-запросы выборки задач с использованием операторов `WHERE` (фильтр), `ORDER BY` (сортировка) и `LIMIT/OFFSET` (пагинация). - **ОТЛОЖЕНО** *(работа инженера БД)*
* [ ] **[Backend(Я)]** Принять query-параметры из URL-адреса (`status`, `sort`, `page`, `limit`) в FastAPI и передать их в SQL-запрос инженера. - **ОТЛОЖЕНО** *(моя задача на этапе написания CRUD)*

---

## 🟥 6. Реализовать модуль для CRUD операций
> **Описание из ТЗ:** Реализовать модуль для CRUD операций.
* [x] **[Backend(Я)]** Написать заглушки для всех 4-х CRUD-роутов задач в FastAPI для симуляции работы без БД. - **ЗАГЛУШКА**
* [ ] **[Database Engineer(Егор)]** Подготовить SQL-шаблоны для операций: `INSERT` (Create), `SELECT` (Read), `UPDATE` (Update), `DELETE` (Delete). - **ОТЛОЖЕНО** *(задача инженера БД)*
* [ ] **[Backend(Я)]** Связать CRUD-роуты с реальными SQL-запросами после готовности базы данных. - **ОТЛОЖЕНО**

---

## 🟨 7. Обязательные поля для сущности User
> **Описание из ТЗ:** Вне зависимости от задачи в базе храниться сущности типа User со следующими полями как минимум: user_name, password_hash или token.
* [x] **[Backend(Я)]** Создана модель данных `UserRegister`, принимающая `username` и `password`. - **ВЫПОЛНЕНО**
* [ ] **[Database Engineer(Егор)]** Создать в базе данных таблицу `users` строго с полями `id`, `user_name`, `password_hash` и `token`. - **ОТЛОЖЕНО** *(работа инженера БД)*
* [ ] **[Backend(Я)]** Переписать логику регистрации, чтобы записывать `password_hash` вместо сырого пароля, и генерировать `token`. - **ОТЛОЖЕНО** *(после развёртывания БД)*

---

## 🟥 8. Защита роутов от неавторизованных пользователей
> **Описание из ТЗ:** Защитить роуты, там где нужно, от не авторизованных пользователей.
* [ ] **[Backend(Я)]** Написать функцию-секьюрити (Middleware / Depends) для проверки валидности JWT-токена в заголовках запроса. - **ОТЛОЖЕНО** *(пишется после реализации выдачи JWT)*
* [ ] **[Backend(Я)]** Закрыть защитные роуты CRUD (`POST`, `PUT`, `DELETE` для задач) этой проверкой, чтобы анонимы не могли менять данные. - **ОТЛОЖЕНО**

---

## 🛑 9. Запрет на использование ORM
> **Описание из ТЗ:** Запрещено использовать ORM, только SQL запросы.
* [ ] **[Backend(Я)]** Установить драйвер базы данных (`pymysql` или `mysql-connector-python` или что душе угодно) в виртуальное окружение Core. - **ОТЛОЖЕНО**
* [ ] **[Backend + DB Engineer(Мы с Егорчезом)]** Написать модуль `database.py` или типо того, для ручного открытия/закрытия соединений с базой данных. - **ОТЛОЖЕНО**
* [ ] **[Backend(Я)]** Все CRUD-операции и логику авторизации писать строго через сырые текстовые SQL-запросы (`execute("SELECT...")`). - **ОТЛОЖЕНО**

---

## 🟥 10. Репликация Базы Данных Master-Slave
> **Описание из ТЗ:** Реализовать репликацию для основной базы данных Master-Slave.
* [ ] **[Database Engineer(Егор)]** Настроить конфигурационные файлы базы данных для разделения серверов на Master (для записи) и Slave (для чтения). - **ОТЛОЖЕНО** *(чисто инфраструктурная задача инженера БД)*
* [ ] **[Database Engineer(Егор)]** Настроить автоматическую синхронизацию данных между Master и Slave базами. - **ОТЛОЖЕНО**

---

## 🟨 11. Эндпоинт /dashboard с диаграммами (Chart.js / Highcharts)
> **Описание из ТЗ:** Реализовать эндпоинт /dashboard c диаграммами для аналитика c использованием highcharts или chartjs.
* [x] **[Backend(Я)]** Реализован JSON-роут `/api/analytics` во Flask для отдачи сырых статистических данных. - **ЗАГЛУШКА** *(отдаёт фейковые цифры для тестов)*
* [ ] **[Backend(Я)]** Переписать роут аналитики на сбор реальных данных из базы данных (подсчёт количества задач по статусам и прочее по необходимости), когда Database Engineer развернёт базу данных. - **ОТЛОЖЕНО**
* [ ] **[Frontend(Тимур)]** Создать страницу `/dashboard`, подключить библиотеку **Chart.js** (или Highcharts) и визуализировать JSON-данные от бэкенда в виде графиков/диаграмм. - **ОТЛОЖЕНО** *(задача фронтендера)*

---

## 🟨 12. Профиль пользователя и обновление токена + защита
> **Описание из ТЗ:** Реализовать эндпоинт /<username> c информацией о пользователе (любой контекст) с кнопочной обновления токена роут должен быть защищен.
* [x] **[Backend(Я)]** Написан JSON-роут `GET /api/users/{username}` для отдачи лорного контекста сотрудника. - **ЗАГЛУШКА**
* [x] **[Backend(Я)]** Написан роут `POST /api/users/{username}/refresh-token` для имитации обновления JWT-токена. - **ЗАГЛУШКА**
* [ ] **[Backend(Я)]** Повесить функцию-секьюрити (Middleware / Depends) для защиты этого роута от анонимов. - **ОТЛОЖЕНО** *(после реализации JWT)*
* [ ] **[Frontend(Тимур)]** Создать страницу профиля `/<username>` (без приставки /api/), вывести туда контекст от бэкенда и сделать кнопку обновления токена. - **ОТЛОЖЕНО** *(задача фронтендера)*

---

## 🟩 13. Информационный роут /api/about
> **Описание из ТЗ:** роут /api/about возвращает json - данные о проекте должны быть в about.json или about.toml.
* [x] **[Backend(Я)]** Создан файл данных `about.json` с описанием команды и проекта. - **ВЫПОЛНЕНО**
* [x] **[Backend(Я)]** Реализован роут `GET /api/about` (`GET /api/v1/supporting/about` в нашем случае) во Flask, который динамически читает файл `about.json`. - **ВЫПОЛНЕНО**
* [x] **[Backend(Я)]** Написан резервный ответ (Fallback) на случай отсутствия файла. - **ВЫПОЛНЕНО**

---

## 🟩 14. Эндпоинт хэширования строк
> **Описание из ТЗ:** Реализовать эндпоинт (/api/hash/{str}) принимает строку и должен вернуть hash строки в формате json.
* [x] **[Backend]** Реализован роут `GET/api/hash/{str}` (`GET /api/v1/supporting/hash/{str}` в нашем случае) во Flask. - **ВЫПОЛНЕНО**
* [x] **[Backend]** Написана логика шифрования через алгоритм SHA-256 (библиотека `hashlib`). - **ВЫПОЛНЕНО**
* [x] **[Backend]** Ответ сервера строго соответствует формату ТЗ (`request`/`result`). - **ВЫПОЛНЕНО**
